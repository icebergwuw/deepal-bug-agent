# 2026-07-21 Bug 表可读性规则

## 反馈

- 吴优反馈 ADS-47165 的 D 列使用 `pending态`、`绑定`、`可见调度` 等内部术语，单独阅读无法直接理解。
- 根因不是程序截断；原约 70 字为软上限，但压缩时错误保留了技术术语。

## 规则更新

- `agent/sheet-contract.md`：明确可读性高于字数，D 必须单独说明场景、用户动作、目标行为和责任方；D/H 禁止未解释的内部字段和缩写。
- `agent/workflows/bug.md`：交付前增加 D 列脱离 C 的单独阅读检查。
- `/Users/you.wu/.codex/skills/deepal-product-bug-handler/SKILL.md`：同步“可读性优先、内部术语翻译”要求，确保新 session 触发 Skill 后执行。

## 已修正案例

- Jira：ADS-47165。
- 负责人：吴优。
- 表格：`bug!C140/D140/I140`。
- D：`解决：替换途经点时，“第一个”只选择左侧搜索结果中的第一个地点；语音交互研发处理。`

## 校验

- 已回读三个规则入口，均包含可读性优先和 D 列单独阅读要求。
- 已回读 ADS-47165 表格内容和本日志。
