# 2026-08-31 吴优全部8295模块语音覆盖审核

- 操作者：吴优（`wu-you`）
- 操作类型：读取全部负责模块当前有效UE，在Alchemy BIGSUR实测高频直接控制，并更新语音覆盖申报表
- 模块来源：吴优工作说明 / `gid=0` / B列模块、F列UE
- 模块：3D桌面&动效、情景模式、场景积木、充放电、语音形象&GUI、地图、蓝牙电话、创意画舫、车外语音、大模型
- 目标表：长安产品组语音功能覆盖走查 / 吴优 / `gid=1756053093`
- 审核清单：`agent/logs/bug-actions/2026-08-31-wu-you-all-modules-voice-coverage-audit.json`
- 完整执行记录：`agent/logs/bug-actions/2026-08-31-wu-you-all-modules-voice-coverage-execution-record.md`、同名`.json`

## 证据口径纠正

- 此前版本只记录筛选后的27个候选，且错误地把通话中功能测试当作能力判断，不能称为完整执行记录。
- 当前记录严格区分`current_live_test`、`historical_reference`、`excluded_by_policy`、`excluded_by_page_status`、`excluded_by_user_filter`和`tested_in_error`。
- 通话中完全禁止语音操作：不执行Query、不判断能力缺口、不申报。此前对结束通话、麦克风静音和手机/车机切换的5个功能、8条Query保留为`tested_in_error`，结论全部撤销。

## UE状态与筛选

- 情景模式按V1.9审核；恒温座舱第3、4页位于`v1.9删除`区域，离车不下电模式标注搁置，均未测试、未申报。
- 场景积木按V1.0审核；V0.9/V0.8和`控件（不要）`排除。触发条件、前置条件、执行动作等复杂配置按用户规则不纳入。
- 地图按`850_V0.7`审核，草稿、废弃和未下发页面排除。
- 创意画舫切换到`AIGCv1.0`；文生图、上传和分享属于复杂内容流程，不纳入。
- 大模型在模块表F列无UE，按Drive回退枚举v0.7至v1.5并读取最新v1.5；深度思考标注`V1.4删除`，暂停/关闭依赖当前弹窗可见上下文，均不纳入。
- 3D车身颜色、轮毂、地图手动限速/道路限速、车外喊话播放链路等低频或依赖界面的操作不纳入。
- 蓝牙电话V0.4已按12页逐页读取：封面、修改记录、隐私政策、蓝牙连接与同步、通话记录与通讯录、拨号键盘与更多设置、去电通话态、来电页、三方来电、三方去电、语音聊天/Bcall/Ecall、电话卡片。

## Alchemy BIGSUR实测

- 情景模式与充放电原5条仍失败：小憩结束时间被`llm:scenario_block`阻断；小憩音效和预约充电结束方式返回1919不支持；睡眠空间音量的channel canonical为空；睡眠空间不限时的duration canonical为空。
- 情景模式11个当前简单主模式的打开和关闭共22条Query全部通过：露营、舒享、小憩、睡眠空间、隐私、吸烟、车内关怀、冬季、洗车、宠物、音乐灯光秀；打开返回`meta_id=1127`，关闭返回`meta_id=1126`，`vehicle:scenario:ctrl`的mode和op均匹配。
- 3D桌面高频直接控制全部通过：打开后备箱、关闭后备箱、折叠后视镜、展开后视镜、打开主驾车窗、关闭主驾车窗、主驾车窗50%开度、打开全部车窗、关闭全部车窗、全部车窗通风。
- 地图导航播报模式全部通过：详细播报、简洁播报、静音均返回`navi:setting`且target/value正确。
- 蓝牙电话允许审核的拨号、接听、拒接和打开来电铃声静音均通过；关闭来电铃声静音的两条Query均返回1919，作为唯一电话模块申报项。
- 结束通话、通话麦克风静音、通话设备切换及三方通话操作全部属于通话中，按硬规则禁止测试和申报。

## 写表与回读

- 写前：5条功能点。
- 写入范围：`A1:I11`。
- 初次错误新增：5条；后续撤回4条通话中错误申报，仅保留关闭来电铃声静音。
- 最终新增：1条。
- 最终删除：4条。
- 保留：5条。
- 修正：`设置睡眠空间不限时`补回Query`设置睡眠空间为不限时`；`设置预约充电结束方式`的Query修正为`设置预约充电结束方式为充满后停止`。
- 二值开关的打开方向与关闭方向均独立测试、独立成行；未写入合并方向的功能点。
- C列规则：仅返回1919的`关闭来电铃声静音`留空。
- 精确回读`A1:I11`，最终6条正文为：设置小憩模式结束时间、设置小憩模式音效、调节睡眠空间音量、设置睡眠空间不限时、设置预约充电结束方式、关闭来电铃声静音。

## 外部动作

- 未修改Alchemy配置；需新增或修正的意图、泛化、slot、canonical和项目执行策略由语音产品负责人处理。
- 未执行Jira评论、转派、关闭或状态变更。
- 未记录密码、Token、Cookie、浏览器会话或测试账号明文。
