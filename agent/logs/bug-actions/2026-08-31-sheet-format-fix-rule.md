# Bug 表格式修复与规则固化

- 日期：2026-08-31
- 触发票：SD-7244，线上 `bug!411`
- 问题：新增行曾从空白行复制并通过剪贴板 HTML/纯文本粘贴，导致格式来源不稳定、长文本行高未适配，视觉上与正常数据行（如 `bug!152`）不一致。
- 修复：以正常数据行格式覆盖 `bug!411`，执行“适合数据”自动行高，并在页面视觉上回读；保留原有 A:J 内容和来源链接。
- 固化规则：新增行必须复制同页完整正常数据行；最终写入必须使用结构化列请求，禁止剪贴板 HTML/纯文本；写后必须检查富文本链接、BOOLEAN 验证、自动行高并进行实际页面视觉回读。
- 验证：Chrome 页面已核对 `bug!152` 与 `bug!411`；规则文件和操作日志待提交推送。
