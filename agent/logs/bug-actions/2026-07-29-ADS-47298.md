# ADS-47298 产品核验与写表记录

- 处理日期：2026-07-29
- 负责人：吴优
- Jira 当前状态：In Progress；未执行 Jira 评论、转派、关闭或状态变更。
- 线上位置：`吴优工作说明 > bug!A151:J151`（写入前以线上空行复核为准）

## 决策核验卡

- 备注因果链：原票在“年终奖很开心，推荐欧美流行音乐，等会去喝奶茶庆祝”中先出现 DEEPAL AI 弹窗、再出现 POI 弹窗，原先语音提前退出已通过代码与 20 次回归修复。当前未交付的问题是 POI 与闲聊复合指令的卡片展示顺序；客户提出“等播报结束后再展示另一弹窗”，研发说明该策略需要按需求统一优化。
- 关联票：已完整读取 ADS-47648（`relates to`，已读）和 ADS-47462（`relates to`，已读），以及 GL-5960（`relates to`，已读）。ADS-47648 明确 POI 推荐与闲聊/天气共存时仅展示 POI 卡片、闲聊/天气仅 TTS；ADS-47462 已修复 DEEPAL AI 弹窗与 POI 卡片互斥错误，并已转入 ADS-47648；GL-5960 为汇总归类票，不含本票目标定义。
- 资料适用范围：① ADS-47298 原票：C673-7、`llm:chat:claw` 复合指令及卡片现象，`exact`。② ADS-47648：同 C673-7、明确 `poi推荐+闲聊` 的 UI/TTS 优先级，`exact`。③ ADS-47462：同类卡片互斥实现修复，但原始组合和表现不同，`partial`。④ GL-5960：仅问题归类，`gap`。
- 冲突处理：客户期望“播完后展示另一弹窗”与 ADS-47648 的正式目标“仅 POI 卡片、闲聊仅 TTS”不一致；以正在 In Progress 的同车型 POI 优先级任务为当前交付目标。原票已修复的语音提前退出不再作为本次 UI 问题的重复缺陷。
- 唯一结论：状态：待回归。结论：本票的POI与闲聊卡片展示应按 ADS-47648 的“仅POI卡片、闲聊仅TTS”目标实现；不再按语音提前退出缺陷重复修复。依据：[Jira原票](http://jira.i-tetris.com/browse/ADS-47298)、[POI优先级ADS-47648](http://jira.i-tetris.com/browse/ADS-47648)、[关联修复ADS-47462](http://jira.i-tetris.com/browse/ADS-47462)。处理：并入ADS-47648完成实现并回归；AI大模型POI推荐研发处理。

## 证据与写表计划

- C 记录原票现象、已修复的提前退出与当前卡片策略的责任边界。
- D 使用上述唯一结论；G 为 `待回归`；I 仅保留 ADS-47648 交付后的复测门槛。
- J 汇总 Jira 原票、ADS-47648、ADS-47462、GL-5960 四个原始入口，均以富文本短标签写入。

## 写后回读

- 已写入 `bug!A151:J151`。A 为 2026-07-29；B 为 ADS-47298 的 HYPERLINK 公式；E 为严格 BOOLEAN `FALSE`；G 为 `待回归`。
- D 回读与核验卡唯一结论逐字一致；C、D、J 的每个短标签均已回读到 Jira 原票、ADS-47648、ADS-47462 或 GL-5960 的正确目标。
- `bug_sheet_contract.py validate-append`：通过；未执行任何 Jira 外部动作。
