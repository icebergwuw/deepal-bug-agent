# 2026-08-17 Drive 检索闭环门禁 v1.14.0-trial.1

- 操作者：Codex
- Run ID：20260817T154826+0800-drive-search-completion-gate
- 涉及 Jira：HUR-81975（回归场景；本次未更新该 Jira 或线上表格）
- 操作类型：规则维护
- 读取证据：项目证据契约、Bug 工作流、v4 manifest 配置与校验脚本、HUR-81975 两份历史复查 manifest
- 表格位置：bug!A186:J186（仅作为问题来源；本次未写入）
- 写入摘要：新增引用文档全名追查、版本族枚举、最新适用版本选择和 `search_completion` 机器门禁；manifest 升级为 schema v5。
- Jira 动作：未评论 Jira / 未流转 Jira
- 回读校验：规则文件与测试变更已回读；线上表格未写入，不适用。
- run bundle：不适用（规则维护，无线上写入）
- readback_sha256：不适用
- 未执行动作：未更新 bug 表 186 行、未更新负责人索引、未评论或流转 Jira；HUR-81975 必须按 v5 完整重跑后才能写表。
- 敏感信息处理：未记录账号、密码、token 或访问口令。

## 根因与处理

- v4 只能证明 Drive 搜索有回执、回执候选已逐项处理，不能证明功能清单或资料索引中的文档名被继续追查。
- v4 不能证明同系列版本已枚举，也不能证明所读文档是最新适用版本；因此宽泛搜索不完整仍可能通过。
- v5 要求每个候选显式登记 `references`，每个引用绑定精确文档名回执；版本化候选必须绑定版本族回执、已读选中候选、选择理由和更新版本检查。

## 验证

- 未追查候选引用：拒绝。
- 没有精确文档名回执：拒绝。
- 未枚举版本族：拒绝。
- 精确名称与最新版本闭环完整：通过。
- 完整项目测试结果以本次提交前终端校验为准。
