# 2026-07-13 ADS 前五条核查

## 触发

- 吴优要求按既有闭环方式核查 ADS 未解决列表前五条。
- 涉及：`ADS-46266`、`ADS-46267`、`ADS-46239`、`ADS-46283`、`ADS-45446`。

## 读取证据

- 完整读取每条 Jira 的标题、状态、描述、附件信息和时间线评论。
- 对 `ADS-46266`、`ADS-46267` 在 Alchemy 在线对话执行原话测试。
- `ADS-46266` 继续读取标准功能 `meta_id=1599` 的详情和项目继承；确认其为“打开定时通风”，未继承深蓝。
- 对 `ADS-46239`、`ADS-45446` 读取记忆空间 PRD；对 `ADS-46266` 读取深蓝空调 PRD。
- 检索本地 `agent/product-kb/`，修正 ADS-46266 旧案例中未完成平台核对的结论。

## 表格写入

- 表格：`吴优工作说明`，页签：`bug`。
- 第 35 行 `ADS-46283`：保留原 D/F/H，仅将状态更新为“已关闭”，备注补充客户在 Jira 确认当前方案无问题、Jira 已解决/Invalid。
- 第 36 行 `ADS-46266`：保留原 D；C/G/H/I/J 更新为在线测试和标准功能证据，最终处理为转需求新增深蓝外循环语音项目点，`1599` 不得复用。
- 第 37 行 `ADS-46267`：写入单操作在线证据，转 DEEPAL AI 车控助手/语音运营配置处理成功后额外“不支持”TTS。
- 第 38 行 `ADS-46239`：写入 PRD 和研发评论证据，结论为关闭无效 bug。
- 第 39 行 `ADS-45446`：写入 PRD 和研发评论证据，结论为关闭无效 bug。

## 回读校验

- 回读 `bug!A35:J39`：五条 Jira 公式完整，D/F 原有内容未被覆盖。
- E35:E39 均为严格 BOOLEAN 复选框，值均为 `FALSE`，未替人工标注准确率。
- 已自动调整第 35 至 39 行行高；基础筛选范围已覆盖 `A1:J39`。
- 同步更新 `agent/bug-index.md` 和产品知识库案例索引。

## 未执行动作

- 未在 Jira 发布评论。
- 未转派、未修改 Jira 状态、未新建需求票。
