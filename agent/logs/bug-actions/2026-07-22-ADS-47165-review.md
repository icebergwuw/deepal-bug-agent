# 2026-07-22 ADS-47165 复盘

## 范围

- Jira：ADS-47165。
- 负责人：吴优。
- 线上位置：`bug!A140:J140`。
- 触发：Jira 在原判断后新增“可见优先级高、非 bug、转产品”的反馈。
- 外部动作：未评论 Jira、未转派、未变更状态。

## 证据

- Jira 当前状态为待办、优先级 P1、经办人为吴优；问题场景为替换途经点时说“第一个”，系统执行了右侧可见卡片而不是本次替换候选。
- Jira 调试证据显示车端本轮 ASR 与 Query 一致，最终命中 `meta_id=1455`、`common:selection`、`list_offset=1`；右侧卡片同时上报 `item_recommend` 和 `match_index:true`。
- Alchemy 标准功能 `meta_id=1455`（标准功能 ID 1425）定义为“选择 - 第N个/带选择条件”；深蓝项目功能点 24544 为“继承未修改”，仅定义选中列表项，未定义多个可见列表同时存在时一律由可见列表优先。
- 2026-07-22 Alchemy 在线三轮复现：首轮“去羽毛球馆，去之前先买杯星巴克”命中 `meta_id=1209`；无车端可见节点和实际澄清状态时，后二轮无法等价复现车端链路，因此以 Jira 同 session 调试结果为主。
- 当前深蓝 8295 大模型 PRD 与 Deepal AI APP PRD 均未发现该多列表优先级定义。
- 追加核验《长安深蓝_8295平台_大模型产品需求文档_POI推荐_v0.8》：2.5.1 明确模型返回 POI 列表后可通过“第x个”按列表项发起导航；2.5.3 明确 POI 列表二次交互场景命中 `meta_id=1455` 时按该列表做特殊处理。该文档可证明序号指令依附当前列表语境，但仍未定义同屏多个列表时“可见列表优先”。

## 产品结论

- 原 D/F/G 保留，复盘最终结论写入 H：`产品补充定义：当系统正在确认替换途经点的候选序号时，“第一个”必须选择本次替换候选列表的第一项，不得被同屏其他可见列表截获；语音交互/可见即可说研发处理。`
- 原 Agent 判断准确：研发提出“可见优先”属于当前实现说明，不是已经核验的正式产品契约，不能据此关闭。

## 写入与回读

- 根据用户提出“依据要体现到 Bug 表”，更新 `bug!C140`、`H140`、`J140`；未改 D、E、F、G、I。
- C 最终回读：`AI大模型PRD/Jira/Alchemy：8295《POI推荐_v0.8》2.5.1规定模型列表可用“第x个”按列表项发起导航，2.5.3规定POI列表二次交互命中meta_id=1455时按该列表处理；本轮Jira命中meta_id=1455、list_offset=1。文档未规定多列表同屏时“可见优先”，当前实现截获了正在确认的途经点候选。`
- J 最终回读为四个可点击证据：`大模型POI PRD`、`Jira同会话日志`、`Alchemy标准1455`、`深蓝项目点24544`，分别指向 8295 POI 推荐 PRD v0.8、Jira 同会话日志、Alchemy 标准功能 1425、深蓝项目功能点 24544。
- 回读确认 B 仍为 ADS-47165 Jira 超链接公式，E 仍为未勾选 BOOLEAN 复选框，G 仍为 `可转语音`。
- H 回读与本日志产品结论完全一致；C/H/J 均保持自动换行，行 140 已恢复“适合数据”自动行高，并在 Google Sheets 页面完成目视校验，证据与结论均可见。
- 2026-07-22 工作区治理复核时，基础筛选已随下一条记录扩展并覆盖 `bug!A1:J141`。
- 本地索引 `agent/bug-owners/wu-you/index.md` 仍定位 `ADS-47165 | 140 | 替换途经点时序号选错列表 | 可转语音`，无需改动。
