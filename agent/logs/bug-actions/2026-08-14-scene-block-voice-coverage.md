# 场景积木语音覆盖审核

- 日期：2026-08-14
- 操作者：吴优（`wu-you`）
- 车型：8295
- 模块：场景积木
- 来源表：`吴优工作说明` / `深蓝8295`，B 列模块、F 列 UE
- UE：MasterGo `149705038244031`，`长安深蓝8295平台交互设计文档_场景积木_V1.0`
- Alchemy：`BIGSUR`
- 目标表：`长安产品组语音功能覆盖走查` / `吴优`

## UE 范围

通过 MasterGo MCP 读取 V1.0 当前 8 个主页面：场景空间、我的场景、触发条件、前置条件、执行动作、新手引导、场景分享、AI 生成场景积木。画布页面列表中的 V0.9、V0.8 属于历史版本，`控件（不要）` 明确禁止使用，均未测试或申报。

V1.0 内未发现整页删除、搁置、废弃或草稿标记。`热度值（暂时不做）` 不是当前可操作控件，未纳入测试。其余按钮、开关、选择器、滑动条、时间/数值设置及内容操作均按当前有效 UE 审核。

## Alchemy 审核

- 共执行 121 条精确中文测试指令。
- 通过 1 个能力点：`打开场景积木`，返回 `app:ctrl open target=sceneblock`，不申报。
- 汇总 41 个待新增能力点，覆盖场景空间/我的场景操作、触发条件、前置条件、执行动作、新手引导、分享和 AI 生成。
- 配置类控件均未正确写入场景积木配置。大量指令虽然返回了正确的车门、车窗、座椅、空调、媒体、导航或屏幕即时操作，但没有设置所指定的触发条件、前置条件或执行动作，因此按 `misrouted` 处理。
- 创建场景返回 `llm:scenario_block`；分享、分享码生成/复制和 AI 生成仅返回成功文本而无可执行 operation；其余部分操作返回 `common:func:not:supported`、错误目标或空 canonical。

## 写表与回读

- 写前回读 `A1:I56`，确认原有数据止于第 15 行，`A16:I56` 为空。
- 2026-08-14 向 `A16:I56` 写入 41 行，功能模块均为 `场景积木`、说明均为 `新增`、车型均为 `8295`、申报人均为 `吴优`。
- 按一个独立可实现能力点一行汇总；同一控件的席位、模式和数值进入设置项范围，不把 121 条测试口令机械拆成重复行。
- 写后复制回读 `A16:I56`，41 行、9 列与写入 TSV 逐字一致；第 15 行与第 16 行字体、字号、加粗状态一致。
- 原有 `A1:I15` 未修改。

## Skill 复测与修订

- 真实模块复测发现原 Skill 只核对 target/action/value，无法区分“写入场景配置”和“立即执行底层车控”。
- 增加 `expected_effect=configure/content_operation` 与 `operation_check.context_match`，配置上下文不匹配禁止通过。
- 增加 `tested_queries`，允许一个申报行保留多条精确实测指令。
- 明确表格聚合规则：相同控件的重复位置/数值可合并，不同语义上下文（触发条件、前置条件、执行动作）必须分开。
- 审核 manifest：`agent/logs/bug-actions/2026-08-14-scene-block-voice-coverage-audit.json`。
- 场景积木与情景模式真实 manifest 校验通过；`configure + pass + context_match=false` 负向用例被拒绝。
- Skill 官方 `quick_validate.py`、安装副本逐文件比对、项目规则架构校验和 54 项现有测试均通过。
- 未执行 Jira 评论、转派、关闭或状态变更。
