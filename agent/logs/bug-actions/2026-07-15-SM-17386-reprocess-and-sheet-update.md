# SM-17386 复核与 bug 表更新

- 日期：2026-07-15
- 操作者：Codex（吴优产品侧）
- Jira：`SM-17386`
- 操作类型：完整复核后更新 `bug!51`；未评论 Jira、未转派、未改变 Jira 状态。

## 读取证据

- Jira 完整字段：C673-G（有网）、CDC、SWD.4.53，状态处理中，P2->Medium，必现 5/5；实际为退出小憩后大灯保持进入前状态，预期为恢复自动大灯。
- Jira 评论按时间线读取 4 条；其中明确“小憩模式不控制大灯”，客户仍要求确认车机逻辑。
- 附件截图与 Jira 描述中的实际现象一致。
- `深蓝情景模式车型配置_20250516` 的 `情景模式` 页：小憩模式定义了开启前置条件和退出，但退出条目未定义大灯状态恢复。

## 写入内容

- `C51`：补充车型、版本、复现结果、评论结论和情景模式配置证据。
- `D51`：当前现状符合旧定义，不判现有实现故障；按客户预期新增规则为“退出小憩后大灯模式置为自动，进入小憩不改灯”，补齐边界后转情景模式/车身灯控研发评估，不直接关闭。
- `G51`：更新为 `转需求评估`。
- `I51`：补充责任边界及需明确的进入前手动状态、手动改灯、异常退出和下电边界。
- `J51`：补充 Jira 与情景模式配置表可点击链接。
- 保留 `A51/B51/E51/F51/H51` 原值。

## 回读校验

- `B51` HYPERLINK 公式保留。
- `E51` 仍为 BOOLEAN，当前值为 FALSE。
- `C51/D51/G51/I51/J51` 已回读为本次写入内容。

## 项目后续动作

1. 产品补充退出小憩后的大灯目标状态，以及进入前手动状态、退出后手动操作、下电等边界。
2. 需求定稿后拆给情景模式/车身灯控研发评估具体实现和信号责任。
3. 研发完成后按 C673-G、SWD.4.53 回归，确认退出小憩后大灯状态符合新定义，再关闭 Jira。
