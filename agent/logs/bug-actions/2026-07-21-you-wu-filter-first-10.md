# 2026-07-21 吴优筛选器前十条有效 Bug

## 范围

- 来源：吴优提供的跨项目 Jira 筛选器，当前排序。
- 去重：以线上 `bug` 页为准，仅跳过吴优页内已有 Jira；跨负责人重复不处理。
- 结果：跳过前置重复项后，处理前 10 条未收录 Bug，写入 `bug!A127:J136`。
- 外部动作：未评论、未转派、未变更 Jira 状态。

## 证据

- 完整读取各 Jira 描述、附件信息和升序评论时间线。
- 核对深蓝可见配置表、C673-G2 导航定义及恒温座舱 V1.4。
- 首次处理时 Alchemy 在线对话会话失效，因此先使用 Jira request/meta_id 日志和配置证据。用户重新登录后已完成在线补测，结果见下文。

## 写入明细

| Jira | 负责人 | 行号 | 产品动作 | 状态 |
|---|---|---:|---|---|
| SLV-44149 | 吴优 | 127 | 新增打开/关闭导航信息显示语音控制，由语音产品/DS处理 | 转需求 |
| SD-4658 | 吴优 | 128 | 泰语模式收到英文导航文案时按英文音色兜底，由语音TTS处理 | 可转语音 |
| SD-2352 | 吴优 | 129 | 恒温座舱激活后辅助驾驶数据入口置灰，由CarSettings研发处理 | 可转研发 |
| PC-38183 | 吴优 | 130 | 补选择+大众常选等名称型备选标签泛化，由语音云端/NLU处理 | 可转语音 |
| PC-38056 | 吴优 | 131 | 将登出映射为地图账号页退出登录，由语音云端/NLU处理 | 可转语音 |
| PC-38036 | 吴优 | 132 | 已开启时选择避开限行不等于关闭，修正测试预期 | 可关闭 |
| PC-38029 | 吴优 | 133 | 补同意协议为账号协议勾选泛化，由语音云端/NLU处理 | 可转语音 |
| PC-38026 | 吴优 | 134 | 已选中充电站时仅支持隐藏/取消，按状态化配置验收 | 可关闭 |
| PC-37808 | 吴优 | 135 | 显式路线偏好在POI确认后直接按偏好选路，由地图研发处理 | 可转研发 |
| PC-37681 | 吴优 | 136 | 将模式1/2分别映射为恒温座舱正常/低功耗子模式，由语音云端/NLU处理 | 可转语音 |

## Alchemy 补测

- 项目：SLV-44149 使用 SLV；其余语音项使用 BIGSUR。补测日期：2026-07-21。
- SLV-44149，吴优，`bug!127`：`打开导航信息显示` 仍落 `meta_id=1919`，无可见结果；维持转需求。
- SD-4658，吴优，`bug!128`：TTS 运行时无声问题，无用户 Query，不适用在线对话。
- SD-2352，吴优，`bug!129`：非语音交互问题，不适用在线对话。
- PC-38183，吴优，`bug!130`：`选择备选二` 落 `meta_id=1455/list_offset=2`；`选择大众常选` 落 `1919`。序号已支持，仅补名称型标签泛化。标准功能点 `1425`，深蓝继承点 `24544`。
- PC-38056，吴优，`bug!131`：`登出` 当前落 `meta_id=1919`，替换 Jira 旧日志中的 `2820`；处理动作不变。
- PC-38036，吴优，`bug!132`：`选择避开限行` 当前落 `meta_id=1919`，不再误接 Jira 旧日志中的 `1023`；已开启态不配置“选择”，维持关闭。
- PC-38029，吴优，`bug!133`：`同意协议` 落通用确认 `meta_id=1433`，`勾选` 落 `1919`，均未命中协议勾选节点。标准功能点 `1422`，深蓝继承点 `24541`。
- PC-38026，吴优，`bug!134`：`选择充电站` 落 `meta_id=1455/charging_station`，`点击充电站` 落 `1919`；已选中态节点已切换为隐藏/取消，维持关闭。
- PC-37808，吴优，`bug!135`：`导航到天安门走最快的路` 落 `meta_id=1209`，正确下发 `shortest_time`；标准功能要求 POI 二次确认后继续使用偏好，故仍由地图研发修复执行链。标准功能点 `1433`，深蓝继承点 `24552`。
- PC-37681，吴优，`bug!136`：模式1/2均落 `meta_id=1127`，参数均为 `constant_temperature_cabin/open`，未区分正常与低功耗；标准功能点 `1364`，深蓝继承点 `24510` 已定义两种恒温座舱子模式。
- 相关 Alchemy：`https://alchemy.i-tetris.com/#/functional/points/detail/1425`、`https://alchemy.i-tetris.com/#/functional/points/detail/1422`、`https://alchemy.i-tetris.com/#/functional/points/detail/1433`、`https://alchemy.i-tetris.com/#/functional/points/detail/1364`。

## 写后校验

- 线上 A:J、B 列公式、E 列复选框、样式和筛选范围已回读。
- Chrome 刷新后目视确认第 127-136 行内容、链接、复选框、自动行高和筛选边界正常。
- 本地索引同步到 135 条，新增 Jira 与线上行号一一对应。
- Alchemy 补测后再次回读 `bug!B127:J136`；仅修正受新证据影响的 C/D/I/J，未改负责人、状态或 H 列。
