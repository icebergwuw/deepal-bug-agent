# 2026-09-14 吴优当前待处理 Bug 批量完整复查（阻塞记录）

- Run ID：`bug-20260914T-batch-you-wu-recheck`
- 操作者 / 目标负责人：吴优（`wu-you`）
- 查询范围：用户提供的 JQL；Chrome Jira 实时结果共 11 条，全部当前经办人为吴优。
- 入口：操作者当前 Chrome 已登录态；Jira 页面已完整读取字段、描述、附件、问题链接和评论时间线。
- 模式：普通二次复查（`recheck`）；若有写入只能更新 C/D/G/I/J，A/B/E/F/H 保护。
- Jira 外部动作：未评论、未转派、未关闭、未修改状态。
- 线上清单、本地索引：本轮未写入。原因是逐票 schema-v5 证据门禁和写前平台门禁未全部通过，未生成任何 Sheets 请求。

## 实时清单

`SD-6421`、`SD-6404`、`SD-5515`、`PC-39375`、`PC-38942`、`PC-37759`、`PC-37473`、`HUR-84753`、`HUR-78190`、`ADS-50547`、`ADS-44267`。

## 逐票复查摘要

| Jira | 当前证据摘要 | 本轮可形成的处理方向 | 阻塞/下一步 |
|---|---|---|---|
| SD-6421 | VR `Search Parking` 上屏但无结果；评论含 SDK 未调用、`SEARCH PARKING/COFFEE` 语料和新增句式需求；关联 `SD-6991` 已读。 | 倾向 `转需求`，补齐 Search 类句式/系统侧 VR 调用边界。 | 语音票缺 Alchemy 当前原话、标准/项目功能点和新鲜 Drive 回执，不能写表或确定责任方。 |
| SD-6404 | C673 导航卡片英文道路名与下个路口信息重叠；视频附件可读；评论要求产品/UI定义边界。 | 倾向 `可转研发`，按同项目 UE 独立分区修复布局。 | 本轮未取得新鲜 Drive/MasterGo connector 回执；旧历史 manifest 不能替代本次检索，不能直接写回。 |
| SD-5515 | 马来语当前位置原话执行 `meta_id=1006`，返回中文地址后仅播 `ok`；评论确认 1006 下发无误并指向语言/TTS链路。 | 倾向 `可转语音`，补马来语地址播报及本地化字段。 | 语音票缺 Alchemy 当前/标准/项目三段证据与 fresh receipt。 |
| PC-39375 | `几点退潮` 在日志中出现闲聊/天气并行；评论确认标准 1400 不含退潮。 | 倾向 `可转语音`，稳定归天气分类并处理冲突消歧；付费潮汐能力需单独边界。 | 语音票缺 Alchemy 当前/标准/项目 fresh receipt。 |
| PC-38942 | 收藏 POI 后说“第一个”时列表偶发收起；评论说明“第一个”和“选择第一个”是不同交互，原票无统一关闭规则。 | 倾向 `转需求`，统一序号选择、收藏、导航、途经点后的列表关闭规则。 | 需当前专项交互/UE 和 Drive connector 回执；不能复用旧结论直接写。 |
| PC-37759 | 蓝牙已连接、通讯录未授权仍可拨打明确号码；原票仅请求确认隐私授权边界。 | 倾向 `非 Bug`，数字直拨与联系人检索授权边界不同。 | 需当前电话 PRD/权限定义回执；历史 Alchemy 证据已过期，本轮不写回。 |
| PC-37473 | POI 列表页说“打开行车记录仪”后 DVR 打开但 POI 弹窗仍在；评论已指出地图侧页面互斥关系和产品规则缺口。 | 倾向 `待确认` 或 `转需求`，先定义语音/非语音入口统一互斥。 | 需新鲜地图专项 UE/配置回执；旧资料不能作为当前写表门禁凭证。 |
| HUR-84753 | J90A SWD.050 麦克风权限列表无蓝牙电话，关闭麦克风开关后蓝牙电话麦克风仍可用；客户编号 `BG20260911000677`，客户用例字段不完整。 | `待确认`，由吴优向客户补齐测试用例并核对权限对象。 | 隐私 UE 已在 Chrome Drive viewer 打开，但未读到当前车端权限清单/实现回读；客户用例缺版本前置、完整步骤和可复现日志结构。 |
| HUR-78190 | 离线 NLU 命中 `meta_id=1018`、`op=close`，车端却播报并执行打开全程预览；评论涉及 TTS 1022/1056 和前后台判断。 | 倾向 `可转研发` 或 `待会诊`，需拆分执行状态机与 TTS 配置责任。 | 语音票缺 Alchemy 当前/标准/项目 fresh receipt；不能把旧“转语音”直接复用。 |
| ADS-50547 | 客户问题 `LS6C3E1D4TF812043`：庆生过程中唤醒其它卡片未打断；有视频和日志附件，评论称此前无此逻辑。 | `待确认`，先补客户完整测试用例，再定义庆生中断规则。 | 客户用例不完整；语音/可见交互还缺 Alchemy、正式交互和当前实现回读，禁止写表。 |
| ADS-44267 | 客户编号 `ID20260602134722264`：天气卡显示后挂 R，覆盖 360/AVM；评论对“设计如此”和 AVM 优先级存在冲突。 | `待会诊`，补齐 AVM Activity 与天气卡跨域层级仲裁。 | 需新鲜 C673 层级配置/MasterGo 目标图层回执；当前仅能确认冲突，不能升级确定性责任。 |

## 门禁结果

- `python3 agent/scripts/bug_project_preflight.py`：身份与吴优写入范围通过；Jira、Google Drive 回执 fresh；Alchemy 回执过期，MasterGo 回执过期。
- Alchemy Chrome 页面当前为登录表单，未能读取在线对话测试；因此所有 `voice` 画像均必须降级，写前预检也会因要求 `alchemy` fresh receipt 失败。
- 本轮没有生成 schema-v5 manifest、Sheets patch/build 请求或 run final readback；不存在可声称已完成的线上写入。

## 恢复条件

1. 在当前 Chrome 登录 Alchemy，并由 Agent 重新读取每张语音票的原话、classification、operations、slot、`meta_id`、标准功能点和深蓝项目功能点；回执标记 `alchemy` fresh。
2. 对地图/UI/跨域票重新取得 Google Drive connector 搜索回执，打开候选正文并完成精确文档名追查和版本族枚举；需要 MasterGo 的票补读目标页面/图层。
3. 为 `HUR-84753`、`ADS-50547` 补齐客户测试用例或保留 `待确认` 门槛后，再逐票生成 schema-v5 manifest，执行 evidence gate、写前预检、列级 patch、线上回读和 final run 校验。

