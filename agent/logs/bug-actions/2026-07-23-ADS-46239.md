# ADS-46239 重新核验与表格更新

- 日期：2026-07-23
- 负责人：吴优
- 规则版本：`v1.0.0-trial.1`
- Jira：`ADS-46239`
- 表格：`吴优工作说明` / `bug!A38:J38`
- 本地索引：`agent/bug-owners/wu-you/index.md`

## 完整读取

- Jira 当前状态为 `Pending`，经办人为吴优，优先级 P1，车型 C385-5。
- 现象：庆生开始后，触发庆生的当前语音轮次未退出聆听。
- 2026-07-06 谭威评论称庆生 TTS 占用语音通道，按当前实现“设计如此”。
- 2026-07-14 周雄评论“已确认转需求输入”。
- 2026-07-20 客户反馈“需要庆生时退出语音”。
- 2026-07-22 周雄补充：客户要求确认解决方案并对齐。

## 正式资料

- 《记忆空间产品 PRD V0.7》2.2.4.2“生日惊喜祝福”明确：
  - 庆生过程开始后关闭顶部语音框；
  - 庆生过程中仍支持唤醒语音助手；
  - TTS、生日歌等庆生动作继续执行；
  - 退出庆生应用仍按换挡、蓝牙电话、语音拉起其他应用等条件处理。
- V0.6 已包含“庆生过程开始后顶部语音框关闭显示，但庆生过程中仍支持唤醒”的相同定义，V0.7不是为本票临时新增该边界。
- 本问题发生在庆生已成功触发后的语音状态切换，不是意图识别、NLU 泛化或落域问题；Alchemy 结果不会改变“当前聆听轮次是否应结束”的判断，因此本次未以 Alchemy 作为定责依据。

## 产品结论

结论：按现行PRD转研发修复，不按“设计如此”关闭。庆生启动后应结束当前聆听轮次并关闭顶部语音框；庆生TTS/生日歌可继续播放，用户如需交互需重新唤醒，仍满足庆生期间支持唤醒。
依据：记忆空间PRD V0.7 2.2.4.2：https://drive.google.com/file/d/1N6E1AnGl_4MZoQxnANNDrfY850sTX03V/view
客户反馈：http://jira.i-tetris.com/browse/ADS-46239?focusedCommentId=8019737&page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel#comment-8019737
最新方案确认：http://jira.i-tetris.com/browse/ADS-46239?focusedCommentId=8047418&page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel#comment-8047418
处理：转语音客户端（语音状态机/GUI）研发修复“庆生TTS占用导致当前轮次持续聆听/语音框未关闭”的状态切换；责任方为语音客户端（语音状态机/GUI）研发。

## 写入与回读

- 保留 D 列原始 Agent 判断、E 列人工复选框和 F 列负责人判断。
- C 列更新完整证据摘要；PRD、研发评论、转需求输入、客户反馈、最新方案确认均已写为原位可点击链接。
- G 列由 `可关闭` 更新为 `可转研发`。
- H 列写入本次复盘后的唯一最终结论 `final_decision_text`。
- I 列写明“仅结束当前聆听轮次，不退出庆生应用”的边界。
- J 列按“标签 + 完整 URL”逐行展示，并写入 6 个独立链接。
- 已回读 `bug!A38:J38`：B 列 Jira 公式仍有效；E 列仍为 BOOLEAN 校验且未勾选；C/J 的链接元数据完整；H 中包含完整依据 URL；自动行高、换行和单元格选区已核验。
- 本地索引第 38 行状态已同步为 `可转研发`。

## 外部动作

- 未评论 Jira。
- 未转派 Jira。
- 未修改 Jira 状态。

## J 列短链接规则更新

- 用户要求 J 列不再显示完整 URL，只保留可点击的文字说明。
- 规则版本由 `v1.0.0-trial.1` 更新为 `v1.0.0-trial.2`。
- 已将 `bug!J38` 改为 6 个短标签：`Jira原票`、`记忆空间PRD V0.7 2.2.4.2`、`研发“设计如此”评论`、`周雄“转需求输入”评论`、`客户要求退出语音评论`、`最新方案确认评论`。
- 已回读 `bug!J38` 的 6 组 `textFormatRuns[].format.link.uri`；表格实际界面仅显示短标签，点击单元格可展开 6 个链接入口。
- D/H 的 Jira-ready 完整 URL 未修改。

## D 列短链接规则更新

- 用户继续要求 D 列也采用可点击短文字，不显示完整 URL。
- 规则版本由 `v1.0.0-trial.2` 更新为 `v1.0.0-trial.3`；H 在承担复盘最终结论时同步适用。
- 写前重新回读 `bug!A38:J38`，确认当前最终流转说明实际位于 `D38`，`H38` 为空；本次只更新 `D38`，未重复写 H。
- D38 的结论、处理动作和责任方保持不变；三条决定性依据改为 `记忆空间PRD V0.7 2.2.4.2`、`客户要求退出语音评论`、`最新方案确认评论`，文字本身分别绑定原始链接。
- 已回读 D38 的 3 组 `textFormatRuns[].format.link.uri`；实际界面仅显示短标签，点击 D38 可展开 3 个链接。
- 未执行 Jira 评论、转派或状态修改。
