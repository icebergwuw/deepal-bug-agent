# 吴优 Bug 表第 107 行格式锚点规则

- 日期：2026-09-03
- 原因：新增行 413-415 曾复制第 412 行简化格式，缺少第 107 行标准样式的边框、内边距和部分居中规则。
- 规则：吴优 `bug` 页新增行只允许以 `bug!107` 为格式锚点；负责人注册表保存 1-based 行号 107，Sheets 请求使用 0-based 索引 106。
- 写前门禁：`bug_sheet_contract.py build` 自动读取负责人锚点；为 `wu-you` 指定其他格式源时拒绝生成请求。
- 写后门禁：同时回读 `bug!A107:J107` 与新增 A:J，使用 `validate-format` 逐列核对固定格式；链接目标和局部富文本链接样式按原链接门禁另行核验。
- 线上验证：413-415 行已按第 107 行复制格式，并补齐空白 F/H 的左对齐；内容、公式、链接、结论和行号未改变。
- 外部动作：未评论、转派、关闭或变更 Jira；未修改 Alchemy、Drive 或 MasterGo。
