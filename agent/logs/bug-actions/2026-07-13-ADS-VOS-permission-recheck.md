# 2026-07-13 ADS VOS 权限开通后复查

## 触发

- 吴优确认 VOS Debug 权限已开通，要求继续处理此前标记为“待补资料”的 `ADS-45187`、`ADS-45188`。

## 读取证据

- `ADS-45187` VOS 日志：车型 `C6736`，原话“进入擦车模式”，在线识别正确但落 `common:func:not:supported`；对照原话“进入洗车模式/打开洗车模式”均命中 `meta_id=1127`、`vehicle:scenario:ctrl`、`mode=car_wash_mode`。
- `ADS-45187` 继续读取标准功能 `1364` 与深蓝项目功能点 `24510`：深蓝已支持洗车模式，已有泛化为“自动洗车模式”。
- `ADS-45188` VOS 日志：车型 `C3852`，原话“现在我导航的地址是哪里呀”，在线识别正确但落 `common:func:not:supported`；对照原话“现在目的地是哪里”命中 `meta_id=1015`、`navi:query:journey:info`、`topic=dest`。
- `ADS-45188` 继续读取标准功能 `1087` 与深蓝项目功能点 `24315`：项目点已支持高德地图的当前目的地查询。
- 重新打开两条 Jira：均为待办、未解决、模块 `AI Cockpit - Voice/General`，均无 Jira 评论。
- 检索《深蓝8155》未命中洗车或当前目的地的独立资料，结论以 VOS、运营平台标准功能和深蓝项目功能点为准。

## 表格写入

- 表格：`吴优工作说明`，页签：`bug`。
- 第 48、49 行：保留 D 列原始“待补日志权限”判断和 E/F 人工区域；C 写入新证据，G 更新为“可转语音”，H 写入复核后的最终处理方式，I/J 补充回归范围和证据链接。
- 已自动调整第 48、49 行行高。

## 回读校验

- 回读 `bug!A48:J49`：B 列 Jira 公式完整，D 列未覆盖。
- E48:E49 均为严格 BOOLEAN 复选框，值仍为 `FALSE`。
- 已同步 `agent/bug-index.md` 和产品知识库案例索引。

## 未执行动作

- 未在 Jira 发布评论。
- 未转派、未修改 Jira 状态、未新建需求票。
