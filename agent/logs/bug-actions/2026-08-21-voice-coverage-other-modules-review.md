# 2026-08-21 语音覆盖其他负责模块复核

- 操作者：吴优（`wu-you`）
- 操作类型：补查深蓝8295负责模块 UE；沉淀开关拆分规则
- 模块来源：吴优工作说明 / 深蓝8295 / B列模块、F列UE
- 目标表：长安产品组语音功能覆盖走查 / 吴优 / gid `1756053093`

## 规则沉淀

- `audit-ue-voice-coverage` 本地源码升级为 `1.0.1`：二值开关必须拆成独立的打开、关闭 control，分别测试、分别记录 verdict、分别决定是否写表。
- `name`、`query`、`tested_queries` 出现“打开/关闭”“开启/关闭”“关闭/打开”等合并表达时，`validate_audit.py` 必须拒绝。
- 正向拆分用例与反向合并用例通过；情景模式、场景积木、充放电三份历史真实 manifest 继续通过。
- 本地源码提交：`a41bf00`；已安装到 `~/.codex/skills/audit-ue-voice-coverage` 并逐文件校验一致。
- GitHub 未同步：SSH 远端返回 `Repository not found`，HTTPS 无可用凭据。因此 Bug 项目外部 Skill 依赖仍保持已发布的 `v1.0.0` / `7fd08f9...`，未伪造更新锁定。

## UE 补查

- 3D桌面&动效：纠正此前误用语音GUI链接的问题，读取 Drive `深蓝汽车_8295平台-3D桌面交互文档v1.1.15-2025_1231.pdf` 全4页及车灯逻辑表。有效控件包括背门、后视镜、车窗、车身颜色和轮毂个性化；状态展示、胎压和车灯逻辑不是独立设置。车身颜色/轮毂低频且依赖可见界面，排除；背门、后视镜、车窗作为直接高频车控待 Alchemy 核验。
- 语音形象&GUI：当前 V0.9 是唤醒、收音、Loading、回复及场景形象的系统状态展示，不是用户设置控件，无候选。
- 地图：当前 `850_V0.7` 排除草稿、废弃和未下发页面；发现导航播报模式候选。手动限速/道路限速与车速、偏差值操作依赖当前页面且涉及行车条件，按复杂/可见优先排除。
- 蓝牙电话：当前 V0.4 发现拨号、接听、挂断、麦克风/铃声静音及手机/车机通话切换，均为直接高频操作，待 Alchemy 核验。
- 创意画舫：明确切换到当前 `AIGCv1.0`，内容为文生图、手机图片分享、入口/退出等内容流程，按复杂、可见优先规则排除。
- 车外语音：当前 V0.9 主要为车外喊话入口、播放和音频优先级链路，未确认独立简单开关或数值控件，无候选。
- 大模型：工作模块表 F 列为空；按 Drive 回退搜索完整文档名，枚举到 v0.7、v0.9、v1.0、v1.2、v1.3、v1.4、v1.5，读取最新 `长安深蓝8295平台交互设计文档_语音大模型_v1.5.pdf`。深度思考区域标记 `V1.4删除`，修改记录明确 C385-5/C673-7 删除深度思考；暂停/关闭为弹窗内可见上下文操作，按规则排除。

## 结果与限制

- 本轮新增 0、删除 0、修改 0；未改变线上语音覆盖表。
- 待 Alchemy 核验：导航播报模式；背门打开/关闭；后视镜折叠/展开；车窗打开/关闭/开度；拨号、接听、挂断、静音及手机/车机通话切换。
- 每个二值开关后续必须按打开、关闭两条分别测试，禁止合并。
- Alchemy 当前显示域账号登录页，未取得实时 operation；在登录完成前不申报上述候选。
- 未执行 Jira 评论、转派、关闭或状态变更；未记录密码、Token、Cookie或测试账号明文。
