# 程云 Bug 工作区

路由状态、Jira 显示名/账号、线上页、页签、gid 和本地索引只以 `../registry.yaml` 的 `cheng-yun` 条目为准，本文件不复制未确认配置。

## 当前状态

- 路由状态：`pending`。
- 已核验身份来源：PC-37473 Jira 当前经办人显示为“程云”，账号为 `yun.cheng`。
- 未确认项：当前在用线上 Bug 清单、准确页签/gid、读写方式、是否允许默认写表。
- 写回边界：未完成上述确认前，只能读取 Jira 和正式资料；不得将其 Jira 写入吴优、李欣、冯智秀或罗稚钦的清单。

## 后续激活条件

- 确认该负责人的当前线上清单与页签，并验证读取和写回方法。
- 完成首次索引同步与同页重复校验。
- 在 `registry.yaml` 中改为 `active + read_write` 后，才可按默认 Bug 流程写表。

## 公共规则

- 公共流程：`../../workflows/bug.md`
- 表格契约：`../../sheet-contract.md`
